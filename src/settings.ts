/**
 * This is the name of the platform that users will use to register the plugin in the Homebridge config.json
 */
export const PLATFORM_NAME = 'SentiotecSaunaHomebridgePlugin';

/**
 * This must match the name of your plugin as defined the package.json
 */
export const PLUGIN_NAME = 'homebridge-sentiotec-plugin';
/**
 * The ID of the sauna which should be queried. Currently only one sauna is supported.
 */
exprot const SAUNA_ID = 0;